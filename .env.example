# Przykładowe zmienne środowiskowe (dokumentacja konfiguracji).
# UWAGA (wersja stub): aplikacja NIE wczytuje pliku .env automatycznie,
# a ten plik ma charakter wyłącznie dokumentacyjny. Stub używa statycznych
# wartości domyślnych z stal/config.py — ustawianie zmiennych środowiskowych
# nie zmienia jeszcze działania aplikacji. Konfiguracja sterowana
# środowiskiem pojawi się w wersji docelowej (real pass).
HOST=127.0.0.1
PORT=5000
SECRET_KEY=zmien-mnie-na-losowy-klucz
FLASK_DEBUG=0
